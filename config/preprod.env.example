# PreProd Environment Variables
# Copy this file and fill in real values — never commit the real file to git

DATABASE_URL=postgresql://library_user:library_pass@preprod-db:5432/library_db
JWT_SECRET=preprod_secret_key_replace_with_strong_random_value_min32
JWT_ALGORITHM=HS256
JWT_EXPIRE_MINUTES=30
FIRST_ADMIN_EMAIL=admin-preprod@library.com
FIRST_ADMIN_PASSWORD=PreProdAdmin@123
FIRST_ADMIN_NAME=PreProd Administrator
